name: Test scroll toolbar anchoring

on:
  push:
    branches: [main]
    paths:
      - tests/editor-word-toolbar-scroll-regression.html
      - editor-formatting.js
      - editor-formatting.css
      - .github/workflows/test-word-toolbar-scroll.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run scrollable modal regression
        run: |
          BROWSER="$(command -v google-chrome || command -v chromium || command -v chromium-browser || true)"
          test -n "$BROWSER"
          timeout 25s "$BROWSER" --headless --no-sandbox --disable-gpu --disable-dev-shm-usage --allow-file-access-from-files --virtual-time-budget=5000 --dump-dom "file://$PWD/tests/editor-word-toolbar-scroll-regression.html" > /tmp/scroll-toolbar.html
          grep 'id="test-output"' /tmp/scroll-toolbar.html
          grep 'data-test-status="pass"' /tmp/scroll-toolbar.html
